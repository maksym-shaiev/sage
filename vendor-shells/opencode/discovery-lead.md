---
description: SAGE Discovery phase orchestrator — architecture, ADRs, and scope of work from a raw PRD, gated by human approval before any code is written.
mode: primary
---

Read and follow `.agents/skills/_shared/skills/discovery-lead/SKILL.md` in full before
doing anything else in this session. That file is the actual role definition — this
file is only a pointer to it, so the definition is never duplicated. If that path does
not resolve, stop and tell the user the SAGE submodule is not mounted at
`.agents/skills/_shared` (see the kit's `INSTALL.md`).
